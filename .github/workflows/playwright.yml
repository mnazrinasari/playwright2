name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:  # This allows manual trigger of the workflow

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Run Playwright tests
      run: npx playwright test

    - name: Install Allure Commandline
      run: |
        # Update and install dependencies
        sudo apt-get update
        sudo apt-get install -y openjdk-11-jdk unzip curl  # Java for Allure and unzip to extract Allure

        # Download and unzip Allure CLI
        curl -o allure-commandline.zip -L "https://github.com/allure-framework/allure2/releases/download/2.19.0/allure-2.19.0.zip"
        unzip allure-commandline.zip -d allure
        
        # List the contents of the unzipped Allure directory to confirm it's there
        ls -l allure/bin

        # Create symbolic link to allure binary in the system PATH
        sudo ln -sf $PWD/allure/bin/allure /usr/local/bin/allure
        
        # Verify allure installation and path
        allure --version  # This should output the Allure version to confirm correct installation
        which allure  # Check if allure is available in PATH

    - name: Generate Allure report
      run: |
        # Check if allure is accessible in the PATH
        which allure  # Ensure it's found in the PATH
        
        # Verify allure version
        allure --version  # Print Allure version for debugging
        
        # Generate the Allure report
        allure generate allure-results --clean -o allure-report

    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: allure-report/
        retention-days: 30

    - name: Install msmtp and send email notification
      if: ${{ always() }}
      run: |
        # Install msmtp
        sudo apt-get update
        sudo apt-get install -y msmtp

        # Clear previous .msmtprc and create new .msmtprc with the correct syntax
        echo "from=${{ secrets.EMAIL_USER }}" > ~/.msmtprc
        echo "auth=login" >> ~/.msmtprc
        echo "user=${{ secrets.EMAIL_USER }}" >> ~/.msmtprc
        echo "password=${{ secrets.EMAIL_PASSWORD }}" >> ~/.msmtprc
        echo "host=smtp.gmail.com" >> ~/.msmtprc
        echo "port=587" >> ~/.msmtprc
        echo "tls=on" >> ~/.msmtprc
        echo "logfile=/tmp/msmtp.log" >> ~/.msmtprc

        # Debug: Output the contents of .msmtprc to check formatting
        echo "Checking contents of .msmtprc:"
        cat ~/.msmtprc

        # Check for hidden characters or formatting issues
        echo "Checking for hidden characters in .msmtprc:"
        cat -v ~/.msmtprc

        # Verify file permissions for .msmtprc (to ensure msmtp can access it)
        ls -l ~/.msmtprc

        # Send email notification (single email address for now)
        echo -e "Subject: GitHub Actions Test Completion Report\n\nYour Playwright tests have completed successfully.\n\nVisit the report: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}" | msmtp "${{ secrets.NOTIFY_EMAIL }}"
